{
  "name": "portfolio-site",
  "private": true,
  "version": "0.0.0",
  "description": "Static portfolio shell for Thanh's portfolio site.",
  "scripts": {
    "build:content": "node scripts/build-index-content.mjs",
    "dev": "python3 -m http.server 4173 -d public",
    "test:responsive": "node scripts/responsive-check.mjs",
    "storybook": "storybook dev --port 6006 --host 127.0.0.1",
    "build-storybook": "storybook build"
  },
  "devDependencies": {
    "@storybook/html-vite": "^10.3.6",
    "detect-port": "^2.1.0",
    "storybook": "^10.3.6"
  }
}
